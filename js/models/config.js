/* ---  CONFIG MODEL CLASS  --- */
WIZARD.Config = Backbone.Model.extend({});

/*---------------  CONFIG MODEL ----------------*/
WIZARD.config = new WIZARD.Config({
    baseUrl: 'http://www.sample.com'
});
